# Archived Parser Versions

These files are **not active** and are kept for historical reference only.

| File | Status | Notes |
|------|--------|-------|
| `integrated_legal_framework_parser.py` | Archived (v1) | Active `app.py` previously pointed here; superseded by v3 |
| `integrated_legal_framework_parser_v2.py` | Archived (v2) | Intermediate iteration; no active consumer |
| `v4.py` | Archived (v4) | Contains richest canonical dataclass model (CanonicalViolation etc.); to be extracted into `domain/` in Phase 3 |

The canonical parser is `ingestors/integrated_legal_framework_parser_v3.py`.
The canonical dataclass entities from `v4.py` will be migrated to `domain/entities/` in Phase 3.
