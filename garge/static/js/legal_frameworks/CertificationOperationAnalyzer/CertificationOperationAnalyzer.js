
/**
 * Generated by framework_parser.py for: Certificación y Operación de Aeronaves Experimentales Construidas por Aficionados, DAN 08 07
 * This function injects a complete analysis panel into a target container.
 * @param {targetContainerId: string, getTranscriptText: function, onAnalyze: function} options
 * - targetContainerId: The ID of the DOM element where the panel's button should be injected.
 * - getTranscriptText: A function that returns the current transcript text to be analyzed.
 * - onAnalyze: A callback function that receives the full prompt package for AI processing.
 */
function injectCertificationOperationPanel(options) {
    const { targetContainerId, getTranscriptText, onAnalyze } = options;

    const uiText = {
        title: 'Certificación y Operación de Aeronaves Experimentales Construidas por Aficionados, DAN 08 07 Analysis',
        description: 'Analyze the current transcript for specific violations of the Certificación y Operación de Aeronaves Experimentales Construidas por Aficionados, DAN 08 07. You can edit the prompt below.',
        buttonText: 'Analyze with Certificación y Operación de Aeronaves Experimentales Construidas por Aficionados, DAN 08 07',
        defaultPrompt: `Analyze the provided transcript and identify behaviors that represent specific violations of the Certificación y Operación de Aeronaves Experimentales Construidas por Aficionados, DAN 08 07.

KEY SECTIONS FOR ANALYSIS:
- **Construction Standards**: 
- **Certification Requirements**: 
- **Operational Procedures**: bajo condición de vuelo ………… ………., según las 
disposiciones de la norma …………… para la utilización señalada más abajo por su explotador.  
 
NOMBRE Y FIRMA DEL CERTIFICADOR AUTORIZADO        NOMBRE Y FIRMA DEL REPRESENTANTE C. CALIDAD  
            DEL CMA / CMAE / FABRICA            TIPO Y NUMERO DE LICENCIA  
          TIPO Y NUMERO DE LICENCIA       TIMBRE DEL CMA / CMAE / FABRICA  
 
 
Como explotador, tomo conocimiento y declaro la siguiente utilización de la aeronave:  
(aeronaves comercial...
- **Maintenance Procedures**: .  
Aprobación DGAC    Firma y Nombre del Constructor y Operador  
Fecha:   
  DAN 08 07  
42 ED 3 / JUL 2023    
DAN 08 07  
43 ED 3 / JUL 2023    
DAN 08 07  
44 ED 3 / JUL 2023    
DAN 08 07  
45 ED 3 / JUL 2023
- **Builder Responsibilities**: 2.2.1  El constructor puede seleccionar su propio diseño y no está sujeto a cumplir con 
estándares de diseño y construcción de aeronaves. A la DGAC no le corresponde 
aprobar el diseño que seleccione el constructor; sin embargo, al constructor se le 
recomienda que:  
2.2.1.1  En el caso de optar por un  diseño original, obtenga asesoría especializada en diseño 
de aeronaves en la medida que estime que la necesita para aclarar cualquier duda. 
Al respecto, existe en la Internet, amplia informac...
- **Aircraft Registration**: 2.7.1  El propietario de la aeronave o quien lo represente, cuando la construcción de la 
aeronave se encuentre próxima a finalizar o haya finalizado, excepto en cuanto a la 
instalación  de las marcas de nacionalidad y matrícula, debe inscribirla en el Registro 
Nacional de Aeronaves, oficina que asigna esas marcas. Para tal efecto, debe 
coordinar la inspección indicada en el requisito 2.5.2.4 (c)  y después de pasada esa 
inspección en form a satisfactoria, presentar los siguientes documentos...

FOR EACH POTENTIAL VIOLATION IDENTIFIED:
1. Quote the exact transcript excerpt (with timestamp and speaker).
2. Reference the specific principle, article or section of the framework that was violated.
3. Briefly analyze the nature and severity of the violation.

PROVIDE A FINAL SUMMARY: Does this pattern of behavior indicate that the responsible entity is systematically failing to comply with the Certificación y Operación de Aeronaves Experimentales Construidas por Aficionados, DAN 08 07?`
    };

    const systemPrompt = 'You are a specialized legal and compliance analyst. Your task is to analyze dialogues strictly in the context of Certificación y Operación de Aeronaves Experimentales Construidas por Aficionados, DAN 08 07, identifying specific violations with precise references. Your output must be structured, factual, and cited.';

    const container = document.getElementById(targetContainerId);
    if (!container) {
        console.error(`[Panel Injector] Target container with ID #${targetContainerId} not found.`);
        return;
    }

    // 1. Create the activation button
    const analysisButton = document.createElement('button');
    analysisButton.textContent = uiText.buttonText;
    analysisButton.className = 'btn btn-special-analysis'; // Add your own styling
    container.appendChild(analysisButton);

    // 2. Create the analysis panel (initially hidden)
    const panel = document.createElement('div');
    panel.className = 'analysis-panel'; // Style this class
    panel.style.display = 'none';
    panel.innerHTML = `
        <div class="panel-header">
            <h3>${uiText.title}</h3>
            <button class="close-btn">&times;</button>
        </div>
        <p>${uiText.description}</p>
        <textarea class="prompt-textarea">${uiText.defaultPrompt}</textarea>
        <button class="execute-analysis-btn">Run Analysis</button>
    `;
    document.body.appendChild(panel); // Append to body to act like a modal

    // 3. Add event listeners
    analysisButton.addEventListener('click', () => {
        panel.style.display = 'block';
    });

    panel.querySelector('.close-btn').addEventListener('click', () => {
        panel.style.display = 'none';
    });

    panel.querySelector('.execute-analysis-btn').addEventListener('click', () => {
        if (typeof onAnalyze !== 'function' || typeof getTranscriptText !== 'function') {
            alert('Analysis function not configured correctly.');
            return;
        }
        
        const userPrompt = panel.querySelector('.prompt-textarea').value;
        const transcriptText = getTranscriptText();

        // Package everything for the AI
        const analysisPackage = {
            system_prompt: systemPrompt,
            user_prompt: userPrompt,
            transcript: transcriptText,
            framework: 'Certificación y Operación de Aeronaves Experimentales Construidas por Aficionados, DAN 08 07'
        };

        onAnalyze(analysisPackage);
        panel.style.display = 'none'; // Hide panel after starting analysis
    });

    console.log(`Panel for Certificación y Operación de Aeronaves Experimentales Construidas por Aficionados, DAN 08 07 injected and ready.`);
}
