
/**
 * Generated by framework_parser.py for: Constitution of the Republic of Chile
 * This function injects a complete analysis panel into a target container.
 * @param {targetContainerId: string, getTranscriptText: function, onAnalyze: function} options
 * - targetContainerId: The ID of the DOM element where the panel's button should be injected.
 * - getTranscriptText: A function that returns the current transcript text to be analyzed.
 * - onAnalyze: A callback function that receives the full prompt package for AI processing.
 */
function injectChileConstitutionPanel(options) {
    const { targetContainerId, getTranscriptText, onAnalyze } = options;

    const uiText = {
        title: 'Constitution of the Republic of Chile Analysis',
        description: 'Analyze the current transcript for specific violations of the Constitution of the Republic of Chile. You can edit the prompt below.',
        buttonText: 'Analyze with Constitution of the Republic of Chile',
        defaultPrompt: `Analyze the provided transcript and identify behaviors that represent specific violations of the Constitution of the Republic of Chile.

KEY SECTIONS FOR ANALYSIS:
- **Human Rights and Dignity**: rights
- **Sovereignty and Democracy**: democratic
- **Probity and Transparency**: Public
- **National Security**: Council will correspond to
the President of the Republic.
The Senate shall appoint three Justices of the Constitutional Court, two directly and the third
one after a previous proposal by the House of Representatives. The latter shall remain in office until
the day on which the currently appointed by the Senate or who replaces him ceases in office, in
accordance with paragraph seven of this article, and can be reappointed.
The current Justices of the Supreme Court, who are at the same of th...
- **Equal Opportunities**: of access to it as well as the
training and improvement of its members.
Any person who is disrupted in his rights by the Administration of the State, its organisms or
municipalities, will be able to complain before the courts that the law establishes, notwithstanding
the responsibility which could affect the functionary that caused the damage.
ARTICLE 38 BIS. The salaries of the President of the Republic, of the senators and representatives,
of the regional governors, of the officials of e...
- **Decentralization**: ed
body of the intendant. It will be headed by a governor, who shall be appointed and removed freely
by the President of the Republic.
49CONSTITUTION OF THE REPUBLIC OF CHILE
It is up to the governor to exercise, according to instructions of the intendant, the supervision of
the public services that exist in the province. The law shall determine the powers that the intendant
may delegate to him and the others that correspond to him.
ARTICLE 117. The governors, in the cases and manner pres...

FOR EACH POTENTIAL VIOLATION IDENTIFIED:
1. Quote the exact transcript excerpt (with timestamp and speaker).
2. Reference the specific principle, article or section of the framework that was violated.
3. Briefly analyze the nature and severity of the violation.

PROVIDE A FINAL SUMMARY: Does this pattern of behavior indicate that the responsible entity is systematically failing to comply with the Constitution of the Republic of Chile?`
    };

    const systemPrompt = 'You are a specialized legal and compliance analyst. Your task is to analyze dialogues strictly in the context of Constitution of the Republic of Chile, identifying specific violations with precise references. Your output must be structured, factual, and cited.';

    const container = document.getElementById(targetContainerId);
    if (!container) {
        console.error(`[Panel Injector] Target container with ID #${targetContainerId} not found.`);
        return;
    }

    // 1. Create the activation button
    const analysisButton = document.createElement('button');
    analysisButton.textContent = uiText.buttonText;
    analysisButton.className = 'btn btn-special-analysis'; // Add your own styling
    container.appendChild(analysisButton);

    // 2. Create the analysis panel (initially hidden)
    const panel = document.createElement('div');
    panel.className = 'analysis-panel'; // Style this class
    panel.style.display = 'none';
    panel.innerHTML = `
        <div class="panel-header">
            <h3>${uiText.title}</h3>
            <button class="close-btn">&times;</button>
        </div>
        <p>${uiText.description}</p>
        <textarea class="prompt-textarea">${uiText.defaultPrompt}</textarea>
        <button class="execute-analysis-btn">Run Analysis</button>
    `;
    document.body.appendChild(panel); // Append to body to act like a modal

    // 3. Add event listeners
    analysisButton.addEventListener('click', () => {
        panel.style.display = 'block';
    });

    panel.querySelector('.close-btn').addEventListener('click', () => {
        panel.style.display = 'none';
    });

    panel.querySelector('.execute-analysis-btn').addEventListener('click', () => {
        if (typeof onAnalyze !== 'function' || typeof getTranscriptText !== 'function') {
            alert('Analysis function not configured correctly.');
            return;
        }
        
        const userPrompt = panel.querySelector('.prompt-textarea').value;
        const transcriptText = getTranscriptText();

        // Package everything for the AI
        const analysisPackage = {
            system_prompt: systemPrompt,
            user_prompt: userPrompt,
            transcript: transcriptText,
            framework: 'Constitution of the Republic of Chile'
        };

        onAnalyze(analysisPackage);
        panel.style.display = 'none'; // Hide panel after starting analysis
    });

    console.log(`Panel for Constitution of the Republic of Chile injected and ready.`);
}
