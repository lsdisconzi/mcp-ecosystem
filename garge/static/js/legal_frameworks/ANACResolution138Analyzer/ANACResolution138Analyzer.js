
/**
 * Generated by framework_parser.py for: RESOLUÇÃO Nº 138, DE 9 DE MARÇO DE 2010
 * This function injects a complete analysis panel into a target container.
 * @param {targetContainerId: string, getTranscriptText: function, onAnalyze: function} options
 * - targetContainerId: The ID of the DOM element where the panel's button should be injected.
 * - getTranscriptText: A function that returns the current transcript text to be analyzed.
 * - onAnalyze: A callback function that receives the full prompt package for AI processing.
 */
function injectANACResolution138Panel(options) {
    const { targetContainerId, getTranscriptText, onAnalyze } = options;

    const uiText = {
        title: 'RESOLUÇÃO Nº 138, DE 9 DE MARÇO DE 2010 Analysis',
        description: 'Analyze the current transcript for specific violations of the RESOLUÇÃO Nº 138, DE 9 DE MARÇO DE 2010. You can edit the prompt below.',
        buttonText: 'Analyze with RESOLUÇÃO Nº 138, DE 9 DE MARÇO DE 2010',
        defaultPrompt: `Analyze the provided transcript and identify behaviors that represent specific violations of the RESOLUÇÃO Nº 138, DE 9 DE MARÇO DE 2010.

KEY SECTIONS FOR ANALYSIS:
- **Pricing Transparency**: 3
- **Ticket Information Requirements**: O bilhete de passagem deverá conter, no mínimo, as seguintes informações, independentemente de sua forma de emissão:


 


I - nome e sobrenome do passageiro;


 


II - nome da empresa aérea emissora;


 


III - lugar e data da emissão;


 


IV - itinerário da viagem;


 


V - horário e data do serviço a ser prestado, salvo nos casos em que a aquisição do bilhete não estiver atrelada a uma data específica para prestação do serviço, que poderá ser escolhida pelo ...
- **Ticket Validity and Transfer**: // Content for Ticket Validity and Transfer not automatically extracted. Please add manual description based on Art\.\s*(10|11)º.
- **Application Scope**: Esta Resolução se aplica aos serviços de transporte aéreo de passageiro com origem no Brasil realizados por empresas nacionais e estrangeiras que operam voos regulares ou não-regulares, domésticos ou internacionais.


 


CAPÍTULO I


DA COMERCIALIZAÇÃO DO BILHETE DE PASSAGEM
- **Enforcement and Penalties**: // Content for Enforcement and Penalties not automatically extracted. Please add manual description based on Art\.\s*12º.

FOR EACH POTENTIAL VIOLATION IDENTIFIED:
1. Quote the exact transcript excerpt (with timestamp and speaker).
2. Reference the specific principle, article or section of the framework that was violated.
3. Briefly analyze the nature and severity of the violation.

PROVIDE A FINAL SUMMARY: Does this pattern of behavior indicate that the responsible entity is systematically failing to comply with the RESOLUÇÃO Nº 138, DE 9 DE MARÇO DE 2010?`
    };

    const systemPrompt = 'You are a specialized legal and compliance analyst. Your task is to analyze dialogues strictly in the context of RESOLUÇÃO Nº 138, DE 9 DE MARÇO DE 2010, identifying specific violations with precise references. Your output must be structured, factual, and cited.';

    const container = document.getElementById(targetContainerId);
    if (!container) {
        console.error(`[Panel Injector] Target container with ID #${targetContainerId} not found.`);
        return;
    }

    // 1. Create the activation button
    const analysisButton = document.createElement('button');
    analysisButton.textContent = uiText.buttonText;
    analysisButton.className = 'btn btn-special-analysis'; // Add your own styling
    container.appendChild(analysisButton);

    // 2. Create the analysis panel (initially hidden)
    const panel = document.createElement('div');
    panel.className = 'analysis-panel'; // Style this class
    panel.style.display = 'none';
    panel.innerHTML = `
        <div class="panel-header">
            <h3>${uiText.title}</h3>
            <button class="close-btn">&times;</button>
        </div>
        <p>${uiText.description}</p>
        <textarea class="prompt-textarea">${uiText.defaultPrompt}</textarea>
        <button class="execute-analysis-btn">Run Analysis</button>
    `;
    document.body.appendChild(panel); // Append to body to act like a modal

    // 3. Add event listeners
    analysisButton.addEventListener('click', () => {
        panel.style.display = 'block';
    });

    panel.querySelector('.close-btn').addEventListener('click', () => {
        panel.style.display = 'none';
    });

    panel.querySelector('.execute-analysis-btn').addEventListener('click', () => {
        if (typeof onAnalyze !== 'function' || typeof getTranscriptText !== 'function') {
            alert('Analysis function not configured correctly.');
            return;
        }
        
        const userPrompt = panel.querySelector('.prompt-textarea').value;
        const transcriptText = getTranscriptText();

        // Package everything for the AI
        const analysisPackage = {
            system_prompt: systemPrompt,
            user_prompt: userPrompt,
            transcript: transcriptText,
            framework: 'RESOLUÇÃO Nº 138, DE 9 DE MARÇO DE 2010'
        };

        onAnalyze(analysisPackage);
        panel.style.display = 'none'; // Hide panel after starting analysis
    });

    console.log(`Panel for RESOLUÇÃO Nº 138, DE 9 DE MARÇO DE 2010 injected and ready.`);
}
