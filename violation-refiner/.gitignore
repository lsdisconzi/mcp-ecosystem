__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.venv/
.DS_Store

# Secrets — never commit
.env
.env.*
!.env.example

# Runtime artifacts
.logs/
.run/
build/
# `build/` (trailing slash) does NOT match `build.prev/`, so the stale-bundle
# snapshot was previously committable at 4.6M. Keep it local-only.
build.prev/
build.*/
dist/
.venv-vps-linux
.vscode
.logs
.run