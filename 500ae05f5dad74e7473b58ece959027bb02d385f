CORRECTION — this commit does NOT contain the code changes its message describes.

The body lists 'Introduced _current_result_ids', 'Revised _wait_for_results',
'Updated get_inteiro_links', 'Modified _open_detail' and 'Removed unnecessary
sleep calls'. None of those happened here: 'git show 500ae05 --
juris-search/chile_scraper.py' is empty, and the blob stayed at
53a163d543ecb4de425988d2f92d0233e5e4bef7. The message is the body of
juris-search/.dev/chilean-jurisprudence/05-chile_scraper-playbook.md (a Phase B
*draft* diff) copied verbatim.

This commit is docs-and-artifacts only: 4 files under
juris-search/.dev/chilean-jurisprudence/ plus 9 unrelated
violation-refiner/build/CL-029/** artifacts swept in from another workstream.

The real change is commit 2f4a2cd ('chile: gate result readiness on row-set
change; separate detail wait'), which also carried the four .dev docs. Do not
revert this commit -- the .dev docs the later work depends on are here. Use
'git log --notes' to see this.
