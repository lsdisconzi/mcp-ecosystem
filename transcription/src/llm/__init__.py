"""LLM provider layer — registry, server-side credentials and unified chat routing.

Mirrors the provider/model design used by the Qdrant manager so every UI in the
ecosystem offers the same experience: pick a provider, pick one of its models,
and never type (or see) an API endpoint.
"""
